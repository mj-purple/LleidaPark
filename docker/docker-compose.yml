version: '3.6'
services:
  db:
    image: mysql
    hostname: localhost
    restart: always
    environment:
      MYSQL_DATABASE: parking
      MYSQL_USER: user
      MYSQL_PASSWORD: jupiter
      MYSQL_ROOT_PASSWORD: jupiter*
    ports:
      - 3306:3306

networks:
  proxy:
    external: true
